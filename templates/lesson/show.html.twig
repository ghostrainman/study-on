{% extends 'base.html.twig' %}

{% block title %}{{ lesson.name }} / {{ lesson.course.name }} / StudyOn{% endblock %}

{% block body %}
    <h1>{{ lesson.name }}</h1>
    <a href="{{ path('app_course_show', {'id': lesson.course.id}) }}">← Вернуться к {{ lesson.course.name }}</a>
    <br>
    <p>
        {{ lesson.content }}
    </p>
    <br>
    <a href="{{ path('app_lesson_edit', {'id': lesson.id}) }}">Редактировать урок</a>
    {{ include('lesson/_delete_form.html.twig') }}
{% endblock %}
